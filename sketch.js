function setup() {
  createCanvas(window.innerWidth, window.innerHeight);
}

function draw() {

  background(51);
  textAlign(CENTER);
  // stroke(255);
  // noFill();
  // strokeWeight(4);
  textSize(34);
  text('Kavin Bharathi', width / 2, height / 2);
}
